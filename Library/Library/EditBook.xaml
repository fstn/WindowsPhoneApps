<phone:PhoneApplicationPage
    x:Class="Library.EditBook"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:tookit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d"
    shell:SystemTray.IsVisible="True">

    <phone:PhoneApplicationPage.DataContext>
        <Binding Path="EditBookViewModel" Source="{StaticResource Locator}"/>
    </phone:PhoneApplicationPage.DataContext>
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="{Binding ResourceGetter.Title,Source={StaticResource LocalizedStrings}}" Style="{StaticResource PhoneTextNormalStyle}"/>

            <TextBlock Text="Modifier un livre" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}" FontSize="48"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="10,0,14,-10" Height="594" VerticalAlignment="Bottom">

            <Rectangle HorizontalAlignment="Left" Height="498" Margin="18,0,0,0" VerticalAlignment="Top" Width="428" Canvas.ZIndex="-1000" >
                <Rectangle.Fill>
                    <SolidColorBrush Color="{StaticResource PhoneAccentColor}"/>
                </Rectangle.Fill>
            </Rectangle>

            <TextBlock Text="ISBN" 
                       TextAlignment="Center" 
                       Foreground="White" 
                       FontSize="24" 
                       Style="{StaticResource PhoneTextTitle1Style}"/>
            <TextBox x:Name="ISBN"
                     Text="{Binding ISBN,Mode=TwoWay}" 
                     Margin="43,37,28,479"
                     Background="{StaticResource PhoneTextBoxBrush}"
                     />
            <TextBlock Text="Title" 
                       Margin="10,120,14,427" 
                       TextAlignment="Center" 
                       Foreground="White" 
                       FontSize="24" 
                       Style="{StaticResource PhoneTextTitle1Style}"/>
            <TextBox x:Name="Title" 
                     Text="{Binding Title,Mode=TwoWay}"                      
                     Margin="43,156,28,0" 
                     TextWrapping="Wrap" VerticalAlignment="Top"/>
            <TextBlock Text="Author" 
                       Margin="18,228,6,320"
                       TextAlignment="Center" 
                       Foreground="White"
                       FontSize="24" 
                       Style="{StaticResource PhoneTextTitle1Style}"/>
            <TextBox x:Name="Author" 
                     Text="{Binding Author,Mode=TwoWay}" 
                     HorizontalAlignment="Left" 
                     Margin="43,274,0,0" 
                     TextWrapping="Wrap" 
                     VerticalAlignment="Top" Width="385" 
                     />
            <Button Content="Modifier" HorizontalAlignment="Left" Margin="73,381,0,0" VerticalAlignment="Top" Width="319" Background="#FF323232" Command="{Binding ModifyBook}"/>

        </Grid>
    </Grid>
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton Click="Home_Click" IconUri="/img/appbar.home.png" Text="Home"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>


</phone:PhoneApplicationPage>